---
phase: 01-qingchang
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - frontend/src/app/(main)/notifications/
  - frontend/src/components/ui/NotificationBell.tsx
  - frontend/src/app/(main)/layout.tsx
autonomous: true
requirements:
  - MOD-03
must_haves:
  truths:
    - "访问 /api/notifications/ 返回 404（路由未注册）"
    - "前端导航栏不存在通知铃铛图标和消息下拉列表"
    - "/notifications 页面路由不存在（目录已删除）"
    - "sonner Toast 和 ProgressDashboard 组件保留完整"
  artifacts:
    - path: "backend/app/main.py"
      provides: "_MODULE_NAMES 已移除 notification"
      contains: "不含 notification 字符串"
    - path: "frontend/src/app/(main)/layout.tsx"
      provides: "已移除 NotificationBell import 和 JSX 引用"
  key_links:
    - from: "frontend/src/app/(main)/layout.tsx"
      to: "sonner Toaster"
      via: "全局 Toast 机制保留"
      pattern: "Toaster"
---

<objective>
简化 M16 通知模块：注销后端路由、删除前端通知中心组件和页面，保留 sonner Toast 和长任务进度条（ProgressDashboard）。

Purpose: 消除通知中心/消息列表这一未真正使用的复杂子系统，用已有的 sonner Toast 满足通知需求。
Output:
- backend/app/main.py：_MODULE_NAMES 移除 notification
- frontend/src/components/ui/NotificationBell.tsx：删除文件
- frontend/src/app/(main)/notifications/：删除目录
- frontend/src/app/(main)/layout.tsx：移除 NotificationBell import 和 JSX
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-qingchang/01-RESEARCH.md

<interfaces>
<!-- layout.tsx 相关结构（摘自代码分析） -->
<!--
import { NotificationBell } from '@/components/ui/NotificationBell';
...
<NotificationBell />   // 在顶栏 JSX 中，第 124 行
-->

<!-- sonner 已全局挂载，位于根 layout.tsx 或 providers -->
<!-- ProgressDashboard 组件保留，负责长任务进度条，不在此 Plan 中触碰 -->

<!-- 后端路由注册（backend/app/main.py _MODULE_NAMES 中） -->
<!-- "notification"  ← MOD-03: 删除此行 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 注销通知后端路由并清理前端组件和页面</name>
  <files>
    backend/app/main.py,
    frontend/src/components/ui/NotificationBell.tsx,
    frontend/src/app/(main)/notifications/,
    frontend/src/app/(main)/layout.tsx
  </files>
  <action>
    按以下顺序执行：

    步骤 1 — 后端路由注销：
    编辑 backend/app/main.py，从 _MODULE_NAMES 删除 "notification" 条目。
    执行前先确认：grep -rn "from app.modules.notification" backend/app/ --include="*.py" | grep -v "modules/notification/"
    预期：无跨模块引用。如有，记录并处理（不影响模块文件本身）。

    步骤 2 — 删除前端通知页面：
    rm -rf frontend/src/app/(main)/notifications/
    这是独立路由页面，删除后 /notifications URL 返回 Next.js 404。

    步骤 3 — 删除 NotificationBell 组件文件：
    先检查 NotificationBell.tsx 是否在其他组件中被 import：
    grep -rn "NotificationBell" frontend/src/ --include="*.tsx" --include="*.ts"
    预期只有 layout.tsx 引用。

    步骤 4 — 清理 layout.tsx：
    编辑 frontend/src/app/(main)/layout.tsx：
    a) 删除 import { NotificationBell } from '@/components/ui/NotificationBell' 这一行
    b) 删除 JSX 中的 <NotificationBell /> 元素（约第 124 行）
    c) 不添加任何替代 UI 元素（通知通过 sonner toast() 呈现，无需顶栏图标）

    步骤 5 — 删除组件文件：
    rm frontend/src/components/ui/NotificationBell.tsx

    验证 sonner Toaster 仍然存在（应在根 layout 或 providers 中）：
    grep -rn "Toaster\|sonner" frontend/src/ --include="*.tsx" | head -5
  </action>
  <verify>
    <automated>
      grep -c "notification" /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend/app/main.py || true
      ls /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend/src/app/\(main\)/notifications/ 2>&1 | grep -q "No such file" && echo "PASS: notifications dir deleted" || echo "FAIL"
      ls /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend/src/components/ui/NotificationBell.tsx 2>&1 | grep -q "No such file" && echo "PASS: Bell deleted" || echo "FAIL"
      grep -c "NotificationBell" /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend/src/app/\(main\)/layout.tsx 2>/dev/null || echo "PASS: no refs"
    </automated>
  </verify>
  <done>
    - _MODULE_NAMES 不含 "notification"
    - /notifications 目录不存在
    - NotificationBell.tsx 文件不存在
    - layout.tsx 中 NotificationBell 的 import 和 JSX 引用均已移除
    - sonner Toaster 保持挂载
    - TypeScript 编译无新增错误：cd frontend && bunx tsc --noEmit
  </done>
</task>

</tasks>

<verification>
1. 后端路由验证：
   curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/api/notifications/  → 预期 404

2. 前端编译验证：
   cd frontend && bunx tsc --noEmit → 无错误

3. 代码检查：
   grep -rn "NotificationBell\|/notifications" frontend/src/ → 无结果

4. sonner 保留确认：
   grep -rn "Toaster\|sonner" frontend/src/ → 有结果（保留）
</verification>

<success_criteria>
- 通知后端路由 /api/notifications/* 返回 404
- 前端无 NotificationBell 组件引用
- /notifications 页面路由不存在
- sonner Toast 和 ProgressDashboard 完整保留
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/01-qingchang/01-02-SUMMARY.md`
</output>
