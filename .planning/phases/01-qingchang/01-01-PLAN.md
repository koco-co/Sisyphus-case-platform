---
phase: 01-qingchang
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - frontend/src/app/(main)/review/page.tsx
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - "访问 /api/test-plans/ 返回 404（路由未注册）"
    - "访问 /api/collaboration/ 返回 404（路由未注册）"
    - "前端不存在 /review 路由（目录已删除）"
    - "DB 表 test_plans 和 collaboration 相关表保持完整，未执行任何迁移"
  artifacts:
    - path: "backend/app/main.py"
      provides: "_MODULE_NAMES 列表已移除 test_plan 和 collaboration"
      contains: "不含 test_plan 和 collaboration 字符串"
  key_links:
    - from: "backend/app/main.py"
      to: "FastAPI router registry"
      via: "_MODULE_NAMES 列表控制注册"
      pattern: "_MODULE_NAMES"
---

<objective>
裁剪 M09（迭代测试计划）和 M18（协作功能）：从后端路由注册表移除，删除前端协作审阅页面。

Purpose: 为 Phase 2 主链路重构清场，消除废弃模块的路由冲突和前端噪声。
Output:
- backend/app/main.py：_MODULE_NAMES 移除 test_plan 和 collaboration
- frontend/src/app/(main)/review/ 目录删除
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-qingchang/01-RESEARCH.md

<interfaces>
<!-- 后端路由注册机制（摘自 backend/app/main.py） -->
<!--
_MODULE_NAMES = [
    "auth", "products", "uda", "import_clean", "diagnosis", "scene_map",
    "generation", "testcases", "diff", "coverage",
    "test_plan",       # MOD-01: 删除此行 → /api/test-plans/* 返回 404
    "templates", "knowledge", "export", "execution", "analytics",
    "notification", "search",
    "collaboration",   # MOD-02: 删除此行 → /api/collaboration/* 不注册
    "dashboard", "audit", "recycle", "ai_config", "tasks",
]
-->

<!-- 前端协作审阅页面（确认存在） -->
<!-- frontend/src/app/(main)/review/ — 需整个目录删除 -->
<!-- frontend/src/app/(main)/layout.tsx — 已确认无 review 路由的 Link 引用 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 从 _MODULE_NAMES 移除 test_plan 和 collaboration</name>
  <files>backend/app/main.py</files>
  <action>
    编辑 backend/app/main.py，从 _MODULE_NAMES 列表中删除两个字符串条目：
    1. 删除 "test_plan" 条目（对应 M09 迭代测试计划）
    2. 删除 "collaboration" 条目（对应 M18 协作功能）

    注意：
    - 不修改模块文件本身（backend/app/modules/test_plan/ 和 backend/app/modules/collaboration/ 目录完整保留）
    - 不执行任何 Alembic 迁移，DB 表保持不变
    - 修改完成后用 grep 确认 _MODULE_NAMES 列表中不再包含 "test_plan" 和 "collaboration"
    - 用 grep 确认 backend/app/ 其他文件（非以上两模块自身）无 "from app.modules.test_plan" 或 "from app.modules.collaboration" 跨模块 import（预期无，但需确认）
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend && grep -n "test_plan\|collaboration" app/main.py | grep "_MODULE_NAMES" || echo "PASS: both removed from _MODULE_NAMES"</automated>
  </verify>
  <done>_MODULE_NAMES 列表中不含 "test_plan" 和 "collaboration"；模块目录文件完整保留；无 DB 迁移执行</done>
</task>

<task type="auto">
  <name>Task 2: 删除前端协作审阅页面目录</name>
  <files>frontend/src/app/(main)/review/</files>
  <action>
    删除 frontend/src/app/(main)/review/ 整个目录（Next.js App Router，删目录即删路由）。

    执行前：
    1. 用 grep 检查整个 frontend/src/ 下是否有文件 import 或 Link 指向 "/review" 路径
       命令：grep -rn '"/review\|href.*review' frontend/src/ --include="*.tsx" --include="*.ts"
    2. 如有引用，先清除引用再删目录

    删除命令：rm -rf frontend/src/app/\(main\)/review/

    删除后：用 ls 确认目录不存在。
    后端 collaboration 模块文件（backend/app/modules/collaboration/）保留不动。
  </action>
  <verify>
    <automated>ls /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend/src/app/\(main\)/review/ 2>&1 | grep -q "No such file" && echo "PASS: review dir deleted" || echo "FAIL: dir still exists"</automated>
  </verify>
  <done>/review 路由目录不存在；前端无其他文件引用 /review 路径；collaboration 后端模块文件完整</done>
</task>

</tasks>

<verification>
1. 后端路由验证（需启动服务）：
   curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/api/test-plans/  → 预期 404
   curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/api/collaboration/  → 预期 404

2. 代码静态验证：
   grep -c "test_plan\|collaboration" backend/app/main.py → 0（_MODULE_NAMES 行不含这两项）

3. 目录验证：
   ls frontend/src/app/\(main\)/review/ → No such file or directory

4. DB 表保留验证（可选，Phase 2 前确认）：
   psql 查询 test_plans 表存在
</verification>

<success_criteria>
- M09 后端路由 /api/test-plans/* 返回 404
- M18 后端路由 /api/collaboration/* 返回 404
- 前端 /review 路由已删除，无相关引用
- 两个模块的 DB 表、模块文件保持完整
- Python lint 通过：cd backend && uv run ruff check app/main.py
</success_criteria>

<output>
After completion, create `.planning/phases/01-qingchang/01-01-SUMMARY.md`
</output>
