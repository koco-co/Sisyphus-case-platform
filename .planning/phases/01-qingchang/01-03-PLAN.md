---
phase: 01-qingchang
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - frontend/src/components/ui/GlobalSearch.tsx
  - backend/app/modules/audit/router.py
  - backend/app/modules/audit/service.py
  - frontend/src/app/(main)/settings/_components/AuditLogs.tsx
  - backend/tests/unit/test_audit/test_audit_filter.py
  - backend/tests/unit/test_search/test_search_types.py
autonomous: true
requirements:
  - MOD-04
  - MOD-05
must_haves:
  truths:
    - "Cmd+K 全局搜索结果只出现「需求」和「用例」两个分组，不出现其他类型"
    - "前端调用 GET /api/audit（而非 /audit/logs），可返回最近 100 条审计日志"
    - "审计日志支持 date_from 和 date_to 时间范围筛选，筛选在后端执行"
    - "设置页时间范围筛选器 UI 可操作，变更后重新请求"
  artifacts:
    - path: "frontend/src/components/ui/GlobalSearch.tsx"
      provides: "GlobalSearch 仅支持 requirement 和 testcase 两种类型"
    - path: "backend/app/modules/audit/router.py"
      provides: "GET /audit 端点支持 date_from/date_to Query 参数"
    - path: "backend/app/modules/audit/service.py"
      provides: "get_audit_logs() 支持 date_from/date_to SQLAlchemy where 过滤"
    - path: "frontend/src/app/(main)/settings/_components/AuditLogs.tsx"
      provides: "API 路径修正 + 时间范围选择器 UI"
    - path: "backend/tests/unit/test_audit/test_audit_filter.py"
      provides: "date_from/date_to 过滤行为的自动化测试"
    - path: "backend/tests/unit/test_search/test_search_types.py"
      provides: "entity_types 限制为 requirement/testcase 的自动化测试"
  key_links:
    - from: "frontend/src/components/ui/GlobalSearch.tsx"
      to: "GET /api/search"
      via: "固定传 types=requirement,testcase 参数"
      pattern: "types.*requirement.*testcase"
    - from: "frontend/src/app/(main)/settings/_components/AuditLogs.tsx"
      to: "GET /api/audit"
      via: "修正路径并附加 date_from/date_to 参数"
      pattern: "/audit\\?"
---

<objective>
功能补完：裁剪全局搜索为「需求+用例」两类（MOD-04），补完审计日志时间范围筛选并修复 API 路径 bug（MOD-05）。先创建测试桩，再实施功能。

Purpose: 搜索去噪（移除 diagnosis/template/knowledge 三类），操作日志合规（支持时间范围筛选，修复 404 bug）。
Output:
- GlobalSearch.tsx 裁剪为 2 种类型
- 后端 audit router + service 添加时间参数
- 前端 AuditLogs.tsx 修复路径 + 添加时间筛选 UI
- 两个测试桩文件
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-qingchang/01-RESEARCH.md

<interfaces>
<!-- GlobalSearch.tsx 当前类型定义（需裁剪） -->
<!--
type ValidType = 'requirement' | 'testcase' | 'diagnosis' | 'template' | 'knowledge';
// 目标：只保留 'requirement' | 'testcase'

const typeConfig = {
  requirement: { label: '需求', icon: FileText, pill: 'pill-blue' },
  testcase: { label: '用例', icon: ClipboardList, pill: 'pill-green' },
  diagnosis: { ... },   // 删除
  template: { ... },    // 删除
  knowledge: { ... },   // 删除
};
// 后端调用时固定：types: 'requirement,testcase'
-->

<!-- AuditLog 后端现状（backend/app/modules/audit/router.py） -->
<!--
GET /api/audit
现有参数：entity_type, action, user_id, page, page_size
缺少：date_from: datetime | None, date_to: datetime | None

前端当前错误路径：/audit/logs?limit=100
正确路径：/audit?page_size=100
-->

<!-- AuditService.get_audit_logs() 现有签名 -->
<!--
async def get_audit_logs(
    self,
    entity_type: str | None = None,
    action: str | None = None,
    user_id: UUID | None = None,
    page: int = 1,
    page_size: int = 50,
) -> tuple[list[AuditLog], int]:
    # 需添加 date_from / date_to 参数
    # 需添加 where(AuditLog.created_at >= date_from) 等过滤
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建测试桩并实施后端 audit 时间范围过滤（MOD-05）</name>
  <files>
    backend/tests/unit/test_audit/test_audit_filter.py,
    backend/tests/unit/test_search/test_search_types.py,
    backend/app/modules/audit/router.py,
    backend/app/modules/audit/service.py
  </files>
  <behavior>
    - 测试 1（audit）：date_from 过滤后只返回该时间之后的日志
    - 测试 2（audit）：date_to 过滤后只返回该时间之前的日志
    - 测试 3（audit）：date_from + date_to 组合过滤
    - 测试 4（search）：调用 search service 时传入非 requirement/testcase 的类型，service 仍正常处理（后端不强制限制，限制在前端）
  </behavior>
  <action>
    步骤 1 — 创建测试桩（RED 阶段）：

    backend/tests/unit/test_audit/test_audit_filter.py：
    使用 AsyncMock mock AsyncSession，测试 AuditService.get_audit_logs() 的时间过滤行为。
    在 service 尚未实现 date_from/date_to 参数时，测试应该 FAIL（参数不被接受）。

    backend/tests/unit/test_search/test_search_types.py：
    验证 SearchService.global_search() 已接受 entity_types 参数并正确过滤。
    参照 test_search_service.py 中已有的 mock 模式。

    步骤 2 — 实施后端 audit 时间范围过滤（GREEN 阶段）：

    编辑 backend/app/modules/audit/service.py：
    在 get_audit_logs() 方法签名中添加：
      date_from: datetime | None = None,
      date_to: datetime | None = None,
    在 SQLAlchemy query 中添加：
      if date_from:
          q = q.where(AuditLog.created_at >= date_from)
          count_q = count_q.where(AuditLog.created_at >= date_from)
      if date_to:
          q = q.where(AuditLog.created_at <= date_to)
          count_q = count_q.where(AuditLog.created_at <= date_to)

    编辑 backend/app/modules/audit/router.py：
    在 list_audit_logs 端点添加 Query 参数：
      date_from: datetime | None = Query(None),
      date_to: datetime | None = Query(None),
      page_size: int = Query(100, ge=1, le=100),  # 默认 100，上限 100
    调用 service 时透传 date_from 和 date_to。

    导入：from datetime import datetime（如未导入）

    步骤 3 — 运行测试（GREEN 验证）：
    cd backend && uv run pytest tests/unit/test_audit/test_audit_filter.py tests/unit/test_search/test_search_types.py -v
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_audit/test_audit_filter.py tests/unit/test_search/test_search_types.py -v</automated>
  </verify>
  <done>
    - 两个测试桩文件存在且测试通过（GREEN）
    - audit service 和 router 支持 date_from/date_to 参数
    - Python lint 通过：uv run ruff check app/modules/audit/
  </done>
</task>

<task type="auto">
  <name>Task 2: 裁剪全局搜索类型 + 修复审计日志前端</name>
  <files>
    frontend/src/components/ui/GlobalSearch.tsx,
    frontend/src/app/(main)/settings/_components/AuditLogs.tsx
  </files>
  <action>
    部分 A — GlobalSearch.tsx 裁剪（MOD-04）：
    1. 修改类型定义，将 ValidType / SearchResult.type 从 5 种缩减为 2 种：
       'requirement' | 'testcase'
    2. typeConfig 对象只保留 requirement 和 testcase 两个 key，删除 diagnosis/template/knowledge
    3. 删除 mockResults 中非 requirement/testcase 类型的条目
    4. 搜索 API 调用处固定传 types 参数：
       { types: 'requirement,testcase' }  或  params.set('types', 'requirement,testcase')
    5. 结果渲染部分确保按类型分组展示（现有逻辑应已支持，确认即可）

    部分 B — AuditLogs.tsx 修复（MOD-05）：
    1. 修复 API 路径：将 /audit/logs 改为 /audit（统一使用 /audit?page_size=100）
    2. 添加 state：
       const [dateFrom, setDateFrom] = useState('');
       const [dateTo, setDateTo] = useState('');
    3. 修改 loadLogs 函数，构建 URLSearchParams：
       params.set('page_size', '100');
       if (dateFrom) params.set('date_from', new Date(dateFrom).toISOString());
       if (dateTo) params.set('date_to', new Date(dateTo + 'T23:59:59').toISOString());
       API 调用：api.get(`/audit?${params}`)
    4. useEffect 依赖添加 dateFrom 和 dateTo（变更时重新请求）
    5. JSX 中在搜索框旁添加两个时间范围输入：
       使用 shadcn/ui Input（type="date"）或原生 <input type="date">
       样式：Tailwind class，与现有 UI 风格一致（bg-sy-bg-2 border-sy-border 等）
       两个输入并排：「开始日期」和「结束日期」，使用 sy-text-3 placeholder
    6. 不改动其他现有过滤器（entity_type、action 下拉等，若存在则保留）

    完成后运行 TypeScript 检查：cd frontend && bunx tsc --noEmit
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit</automated>
  </verify>
  <done>
    - GlobalSearch.tsx 的 ValidType 和 typeConfig 只包含 requirement 和 testcase
    - API 调用固定传 types=requirement,testcase
    - AuditLogs.tsx 调用路径为 /audit（非 /audit/logs）
    - 时间范围筛选器 UI 存在于审计日志列表上方
    - TypeScript 编译无新增错误
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - 全局搜索（Cmd+K）裁剪为仅「需求」和「用例」两类
    - 审计日志页面修复 API 路径并添加时间范围筛选器
  </what-built>
  <how-to-verify>
    1. 启动前端（cd frontend && bun dev）
    2. 按 Cmd+K 唤起全局搜索，输入任意关键词
       预期：只出现「需求」和「用例」两个分组，不出现「诊断」「模板」「知识库」
    3. 访问「设置」→「操作日志」Tab
       预期：页面正常加载日志列表（非 demo 假数据），顶部有时间范围选择器
    4. 选择一个日期范围，预期：列表重新加载并按时间过滤
  </how-to-verify>
  <resume-signal>输入 "approved" 确认验证通过，或描述发现的问题</resume-signal>
</task>

</tasks>

<verification>
1. 后端测试全量：cd backend && uv run pytest tests/unit/test_audit/ tests/unit/test_search/ -v
2. 前端编译：cd frontend && bunx tsc --noEmit
3. Lint：cd backend && uv run ruff check app/modules/audit/ && uv run ruff format app/modules/audit/ --check
4. 手动验证：
   - Cmd+K 搜索只显示 2 种类型
   - /settings 操作日志正常加载 + 时间范围筛选有效
</verification>

<success_criteria>
- MOD-04：全局搜索 Cmd+K 结果仅含「需求」和「用例」两类，按类型分组展示
- MOD-05：审计日志页面正常加载最近 100 条，支持时间范围筛选（后端过滤）
- 后端测试全部通过：pytest tests/unit/test_audit/ tests/unit/test_search/ -v
- TypeScript 编译无错误
- Phase 1 的 5 个需求（MOD-01~05）全部满足
</success_criteria>

<output>
After completion, create `.planning/phases/01-qingchang/01-03-SUMMARY.md`
</output>
