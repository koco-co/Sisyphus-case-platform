# 向量检索和 RAG 实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现向量检索层，支持测试用例向量化存储和相似度检索（RAG）

**Architecture:**
- 使用 sentence-transformers 进行文本向量化
- pgvector 存储向量数据
- 余弦相似度计算
- Few-shot 示例检索

**Tech Stack:** sentence-transformers, pgvector, NumPy

---

## Task 1: 安装和配置向量模型

**Files:**
- Create: `backend/app/rag/embeddings.py`
- Modify: `backend/pyproject.toml`

**Step 1: Update dependencies**

修改 `backend/pyproject.toml`:

```toml
dependencies = [
    # ... 现有依赖
    "sentence-transformers>=2.3.1",
    "numpy>=1.24.0",
]
```

**Step 2: Install dependencies**

```bash
cd backend
uv sync
```

**Step 3: Write failing test for embedding**

创建 `backend/app/tests/test_embeddings.py`:

```python
import pytest
from app.rag.embeddings import EmbeddingModel

@pytest.mark.asyncio
async def test_embedding_model():
    """测试向量化模型"""
    model = EmbeddingModel()
    text = "这是一个测试句子"
    embedding = await model.embed_text(text)

    assert len(embedding) == 384  # MiniLM 模型输出维度
    assert all(isinstance(x, float) for x in embedding)
```

**Step 4: Run test to verify it fails**

```bash
uv run pytest app/tests/test_embeddings.py::test_embedding_model -v
```

Expected: FAIL with "module 'app.rag.embeddings' not found"

**Step 5: Implement embedding model**

创建 `backend/app/rag/__init__.py`:

```python
from app.rag.embeddings import EmbeddingModel
from app.rag.retriever import VectorRetriever
```

创建 `backend/app/rag/embeddings.py`:

```python
from sentence_transformers import SentenceTransformer
from typing import List
import numpy as np

class EmbeddingModel:
    """文本向量化模型"""

    def __init__(self, model_name: str = "paraphrase-multilingual-MiniLM-L12-v2"):
        """
        初始化嵌入模型

        Args:
            model_name: 模型名称，默认使用多语言 MiniLM
        """
        self.model = SentenceTransformer(model_name)
        self.dimension = self.model.get_sentence_embedding_dimension()

    async def embed_text(self, text: str) -> List[float]:
        """
        将文本向量化

        Args:
            text: 输入文本

        Returns:
            向量列表
        """
        embedding = self.model.encode(text, convert_to_numpy=True)
        return embedding.tolist()

    async def embed_batch(self, texts: List[str]) -> List[List[float]]:
        """
        批量向量化

        Args:
            texts: 文本列表

        Returns:
            向量列表
        """
        embeddings = self.model.encode(texts, convert_to_numpy=True)
        return embeddings.tolist()

    def normalize(self, embedding: List[float]) -> List[float]:
        """
        归一化向量（用于余弦相似度计算）

        Args:
            embedding: 原始向量

        Returns:
            归一化后的向量
        """
        arr = np.array(embedding)
        norm = np.linalg.norm(arr)
        if norm == 0:
            return arr.tolist()
        return (arr / norm).tolist()
```

**Step 6: Run tests**

```bash
# 首次运行会下载模型，需要一些时间
uv run pytest app/tests/test_embeddings.py::test_embedding_model -v -s
```

Expected: PASS (模型下载完成后)

**Step 7: Commit**

```bash
git add backend/app/rag/ backend/pyproject.toml
git commit -m "feat: 实现文本向量化模型"
```

---

## Task 2: 更新测试用例模型以支持向量

**Files:**
- Modify: `backend/app/models/test_case.py`
- Create: `backend/app/migrations/add_embedding_column.py`

**Step 1: Write test for embedding column**

创建 `backend/app/tests/test_test_case_embedding.py`:

```python
import pytest
from sqlalchemy import select
from app.database import async_session
from app.models.test_case import TestCase
from app.rag.embeddings import EmbeddingModel

@pytest.mark.asyncio
async def test_save_test_case_with_embedding():
    """测试保存带向量的测试用例"""
    model = EmbeddingModel()
    embedding = await model.embed_text("测试用例内容")

    async with async_session() as db:
        case = TestCase(
            project_id=1,
            title="测试用例",
            steps="测试步骤",
            expected_results="预期结果",
            embedding=embedding
        )
        db.add(case)
        await db.commit()
        await db.refresh(case)

        assert case.embedding is not None
        assert len(case.embedding) == 384
```

**Step 2: Run test to verify it works**

```bash
uv run pytest app/tests/test_test_case_embedding.py::test_save_test_case_with_embedding -v
```

Expected: PASS (如果数据库已支持向量类型)

**Step 3: Create migration script**

创建 `backend/app/migrations/add_embedding_column.py`:

```python
"""添加 embedding 列到 test_cases 表"""

async def upgrade():
    """添加 embedding 列"""
    from app.database import engine
    async with engine.begin() as conn:
        await conn.execute("""
            ALTER TABLE test_cases
            ADD COLUMN IF NOT EXISTS embedding VECTOR(384)
        """)

async def downgrade():
    """移除 embedding 列"""
    from app.database import engine
    async with engine.begin() as conn:
        await conn.execute("""
            ALTER TABLE test_cases
            DROP COLUMN IF EXISTS embedding
        """)

if __name__ == "__main__":
    import asyncio
    asyncio.run(upgrade())
    print("✅ Migration completed")
```

**Step 4: Run migration**

```bash
cd backend
uv run python app/migrations/add_embedding_column.py
```

**Step 5: Verify column exists**

```bash
psql postgresql://sisyphus:password@localhost:5432/sisyphus -c "\d test_cases"
```

Expected: 表中有 `embedding` 列，类型为 `VECTOR(384)`

**Step 6: Commit**

```bash
git add backend/app/models/test_case.py backend/app/migrations/
git commit -m "feat: 为测试用例添加向量存储支持"
```

---

## Task 3: 实现向量检索器

**Files:**
- Create: `backend/app/rag/retriever.py`

**Step 1: Write failing test for vector retrieval**

创建 `backend/app/tests/test_retriever.py`:

```python
import pytest
from app.rag.retriever import VectorRetriever
from app.database import async_session
from app.models.test_case import TestCase

@pytest.mark.asyncio
async def test_vector_retrieval():
    """测试向量检索"""
    retriever = VectorRetriever()

    # 先创建一些测试用例
    async with async_session() as db:
        case1 = TestCase(
            project_id=1,
            title="用户登录测试",
            steps="输入用户名密码",
            expected_results="登录成功",
            status="approved"
        )
        case2 = TestCase(
            project_id=1,
            title="用户注册测试",
            steps="填写注册信息",
            expected_results="注册成功",
            status="approved"
        )
        db.add_all([case1, case2])
        await db.commit()

    # 检索相似用例
    results = await retriever.search_similar_cases(
        query="测试用户登录功能",
        top_k=2,
        status="approved"
    )

    assert len(results) > 0
    assert results[0].title == "用户登录测试"  # 最相似的结果
```

**Step 2: Run test to verify it fails**

```bash
uv run pytest app/tests/test_retriever.py::test_vector_retrieval -v
```

Expected: FAIL with "module 'app.rag.retriever' not found"

**Step 3: Implement vector retriever**

创建 `backend/app/rag/retriever.py`:

```python
from typing import List, Optional
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, text
from app.database import async_session
from app.models.test_case import TestCase
from app.rag.embeddings import EmbeddingModel

class VectorRetriever:
    """向量检索器"""

    def __init__(self):
        self.embedding_model = EmbeddingModel()

    async def search_similar_cases(
        self,
        query: str,
        top_k: int = 5,
        status: str = "approved",
        project_id: Optional[int] = None,
        min_similarity: float = 0.5
    ) -> List[TestCase]:
        """
        检索相似的测试用例

        Args:
            query: 查询文本
            top_k: 返回前 K 个结果
            status: 用例状态过滤
            project_id: 项目 ID 过滤
            min_similarity: 最小相似度阈值

        Returns:
            相似测试用例列表
        """
        # 生成查询向量
        query_vector = await self.embedding_model.embed_text(query)

        # 构建查询条件
        where_conditions = ["status = :status"]
        params = {"status": status, "top_k": top_k}

        if project_id:
            where_conditions.append("project_id = :project_id")
            params["project_id"] = project_id

        where_clause = " AND ".join(where_conditions)

        # 使用 pgvector 的余弦距离搜索
        # 余弦距离 = 1 - 余弦相似度
        sql = f"""
            SELECT id, project_id, module, title, prerequisites,
                   steps, expected_results, keywords, priority,
                   case_type, stage, status, created_at,
                   1 - (embedding <=> :query_vector::vector) as similarity
            FROM test_cases
            WHERE {where_clause}
            AND embedding IS NOT NULL
            ORDER BY embedding <=> :query_vector::vector
            LIMIT :top_k
        """

        async with async_session() as db:
            result = await db.execute(
                text(sql),
                {**params, "query_vector": str(query_vector)}
            )
            rows = result.fetchall()

            # 转换为 TestCase 对象
            cases = []
            for row in rows:
                case = TestCase(
                    id=row.id,
                    project_id=row.project_id,
                    module=row.module,
                    title=row.title,
                    prerequisites=row.prerequisites,
                    steps=row.steps,
                    expected_results=row.expected_results,
                    keywords=row.keywords,
                    priority=row.priority,
                    case_type=row.case_type,
                    stage=row.stage,
                    status=row.status,
                    created_at=row.created_at
                )
                # 添加相似度分数
                case.similarity = row.similarity
                if case.similarity >= min_similarity:
                    cases.append(case)

            return cases

    async def index_test_case(self, case: TestCase):
        """
        为测试用例创建向量索引

        Args:
            case: 测试用例对象
        """
        # 组合文本字段
        text = f"{case.title} {case.steps} {case.expected_results}"

        # 生成向量
        embedding = await self.embedding_model.embed_text(text)

        # 保存到数据库
        async with async_session() as db:
            case.embedding = embedding
            db.add(case)
            await db.commit()

    async def index_batch(self, cases: List[TestCase]):
        """
        批量创建向量索引

        Args:
            cases: 测试用例列表
        """
        texts = [
            f"{case.title} {case.steps} {case.expected_results}"
            for case in cases
        ]

        embeddings = await self.embedding_model.embed_batch(texts)

        async with async_session() as db:
            for case, embedding in zip(cases, embeddings):
                case.embedding = embedding
                db.add(case)
            await db.commit()
```

**Step 4: Run tests**

```bash
uv run pytest app/tests/test_retriever.py::test_vector_retrieval -v
```

Expected: PASS

**Step 5: Commit**

```bash
git add backend/app/rag/retriever.py backend/app/tests/test_retriever.py
git commit -m "feat: 实现向量检索器"
```

---

## Task 4: 创建向量管理 API

**Files:**
- Create: `backend/app/api/vectors.py`
- Modify: `backend/app/main.py`

**Step 1: Write failing test for vector API**

创建 `backend/app/tests/test_vectors_api.py`:

```python
import pytest
from httpx import AsyncClient
from app.main import app

@pytest.mark.asyncio
async def test_index_test_case():
    """测试为测试用例创建索引"""
    async with AsyncClient(app=app, base_url="http://test") as ac:
        # 先创建测试用例
        case_response = await ac.post(
            "/api/cases/",
            json={
                "project_id": 1,
                "title": "测试用例",
                "steps": "测试步骤",
                "expected_results": "预期结果"
            }
        )
        case_id = case_response.json()["id"]

        # 创建向量索引
        response = await ac.post(f"/api/vectors/cases/{case_id}/index")
        assert response.status_code == 200
```

**Step 2: Run test to verify it fails**

```bash
uv run pytest app/tests/test_vectors_api.py::test_index_test_case -v
```

Expected: FAIL with "404 Not Found"

**Step 3: Implement vector API**

创建 `backend/app/api/vectors.py`:

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from pydantic import BaseModel
from typing import List, Optional
from app.database import get_db
from app.models.test_case import TestCase
from app.rag.retriever import VectorRetriever

router = APIRouter(prefix="/api/vectors", tags=["vectors"])

class SearchRequest(BaseModel):
    query: str
    top_k: int = 5
    status: str = "approved"
    project_id: Optional[int] = None
    min_similarity: float = 0.5

class SearchResult(BaseModel):
    id: int
    title: str
    similarity: float
    steps: str
    expected_results: str

@router.post("/cases/{case_id}/index")
async def index_test_case(
    case_id: int,
    db: AsyncSession = Depends(get_db)
):
    """为测试用例创建向量索引"""
    result = await db.execute(
        select(TestCase).where(TestCase.id == case_id)
    )
    case = result.scalar_one_or_none()
    if not case:
        raise HTTPException(status_code=404, detail="测试用例不存在")

    retriever = VectorRetriever()
    await retriever.index_test_case(case)

    return {"message": "向量索引创建成功"}

@router.post("/cases/batch-index")
async def batch_index_test_cases(
    case_ids: List[int],
    db: AsyncSession = Depends(get_db)
):
    """批量创建向量索引"""
    result = await db.execute(
        select(TestCase).where(TestCase.id.in_(case_ids))
    )
    cases = result.scalars().all()

    if not cases:
        raise HTTPException(status_code=404, detail="没有找到测试用例")

    retriever = VectorRetriever()
    await retriever.index_batch(cases)

    return {"message": f"已为 {len(cases)} 条测试用例创建索引"}

@router.post("/search", response_model=List[SearchResult])
async def search_similar_cases(
    request: SearchRequest,
    db: AsyncSession = Depends(get_db)
):
    """检索相似的测试用例"""
    retriever = VectorRetriever()
    cases = await retriever.search_similar_cases(
        query=request.query,
        top_k=request.top_k,
        status=request.status,
        project_id=request.project_id,
        min_similarity=request.min_similarity
    )

    return [
        SearchResult(
            id=case.id,
            title=case.title,
            similarity=getattr(case, 'similarity', 0.0),
            steps=case.steps,
            expected_results=case.expected_results
        )
        for case in cases
    ]
```

修改 `backend/app/main.py`:

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.config import settings
from app.api import projects, cases, config, vectors

app = FastAPI(
    title="Sisyphus API",
    description="测试用例生成平台 API",
    version="0.1.0",
)

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(projects.router)
app.include_router(cases.router)
app.include_router(config.router)
app.include_router(vectors.router)

@app.get("/")
async def root():
    return {
        "message": "Sisyphus 测试用例生成平台 API",
        "version": "0.1.0",
        "status": "running"
    }
```

**Step 4: Run tests**

```bash
uv run pytest app/tests/test_vectors_api.py -v
```

Expected: All PASS

**Step 5: Commit**

```bash
git add backend/app/api/vectors.py backend/app/main.py
git commit -m "feat: 实现向量管理 API"
```

---

## Task 5: 创建 RAG Prompt 构建器

**Files:**
- Create: `backend/app/rag/prompt_builder.py`

**Step 1: Write test for prompt builder**

创建 `backend/app/tests/test_prompt_builder.py`:

```python
import pytest
from app.rag.prompt_builder import PromptBuilder

@pytest.mark.asyncio
async def test_build_few_shot_prompt():
    """测试构建 Few-shot Prompt"""
    builder = PromptBuilder()

    examples = [
        {
            "title": "用户登录测试",
            "steps": "1. 打开登录页面\n2. 输入用户名密码",
            "expected_results": "登录成功"
        }
    ]

    prompt = await builder.build_generation_prompt(
        requirement="用户需要能够登录系统",
        test_points=["验证正常登录", "验证密码错误"],
        examples=examples
    )

    assert "用户需要能够登录系统" in prompt
    assert "验证正常登录" in prompt
    assert "用户登录测试" in prompt
    assert len(prompt) > 100
```

**Step 2: Run test to verify it fails**

```bash
uv run pytest app/tests/test_prompt_builder.py::test_build_few_shot_prompt -v
```

Expected: FAIL with "module 'app.rag.prompt_builder' not found"

**Step 3: Implement prompt builder**

创建 `backend/app/rag/prompt_builder.py`:

```python
from typing import List, Dict, Optional

class PromptBuilder:
    """Prompt 构建器"""

    def __init__(self):
        self.base_prompt = """你是一个资深的测试用例设计专家。

**任务：**
基于以下需求文档，生成高质量、规范的测试用例。

**重要约束（CRITICAL）：**
1. 测试数据必须真实、可用、符合行业实际
2. 禁止使用"比如""例如""XXX""测试数据"等模糊字眼
3. 手机号必须是真实格式（如：13812345678）
4. 身份证号必须是真实格式（如：110101199001011234）
5. 邮箱必须是真实格式（如：test@example.com）
6. 金额必须是合理数值（如：99.99，而非 999999999.99）
7. 日期必须是真实日期（如：2026-03-04）
8. URL 必须是真实格式（如：https://example.com/api/user）

**CSV 格式要求：**
所属模块,用例标题,前置条件,步骤,预期,关键词,优先级,用例类型,适用阶段
"""

    async def build_generation_prompt(
        self,
        requirement: str,
        test_points: List[str],
        examples: Optional[List[Dict]] = None
    ) -> str:
        """
        构建测试用例生成 Prompt

        Args:
            requirement: 需求文档
            test_points: 测试点列表
            examples: Few-shot 示例

        Returns:
            完整的 Prompt
        """
        prompt_parts = [self.base_prompt]

        # 添加历史示例
        if examples:
            prompt_parts.append("\n**历史参考用例（请模仿这些用例的风格和规范）：**\n")
            for i, example in enumerate(examples, 1):
                prompt_parts.append(f"示例 {i}:\n")
                prompt_parts.append(f"所属模块: {example.get('module', '通用')}\n")
                prompt_parts.append(f"用例标题: {example['title']}\n")
                prompt_parts.append(f"前置条件: {example.get('prerequisites', '无')}\n")
                prompt_parts.append(f"步骤: {example['steps']}\n")
                prompt_parts.append(f"预期: {example['expected_results']}\n")
                prompt_parts.append(f"优先级: {example.get('priority', '2')}\n")
                prompt_parts.append(f"用例类型: {example.get('case_type', '功能测试')}\n")
                prompt_parts.append(f"适用阶段: {example.get('stage', '功能测试阶段')}\n\n")

        # 添加需求文档
        prompt_parts.append("\n**需求文档：**\n")
        prompt_parts.append(requirement)
        prompt_parts.append("\n")

        # 添加测试点
        if test_points:
            prompt_parts.append("\n**测试点：**\n")
            for i, point in enumerate(test_points, 1):
                prompt_parts.append(f"{i}. {point}\n")

        # 添加生成要求
        prompt_parts.append("\n**生成要求：**\n")
        prompt_parts.append("请根据上述需求文档和测试点，生成符合规范的测试用例。\n")
        prompt_parts.append("确保每一条用例都是可执行的，测试数据真实可用。\n")
        prompt_parts.append("输出格式为 CSV，可以直接导入测试管理系统。\n")

        return "".join(prompt_parts)

    async def build_review_prompt(self, test_cases: List[Dict]) -> str:
        """
        构建测试用例评审 Prompt

        Args:
            test_cases: 测试用例列表

        Returns:
            评审 Prompt
        """
        prompt = """你是一个资深的测试用例评审专家。

**任务：**
评审以下测试用例的质量。

**评审维度：**
1. **规范性**：是否符合 CSV 格式要求，字段是否完整
2. **完整性**：前置条件、步骤、预期结果是否清晰
3. **可执行性**：步骤是否明确，预期是否可验证
4. **数据真实性**：测试数据是否真实、可用，无模糊字眼
5. **覆盖度**：是否覆盖了主要测试场景

**评审标准：**
- 通过：所有维度都符合要求
- 不通过：有任一维度不符合要求

**待评审用例：**
"""
        for i, case in enumerate(test_cases, 1):
            prompt += f"\n用例 {i}:\n"
            prompt += f"模块: {case.get('module', 'N/A')}\n"
            prompt += f"标题: {case['title']}\n"
            prompt += f"步骤: {case['steps']}\n"
            prompt += f"预期: {case['expected_results']}\n"

        prompt += """

**评审格式：**
请按以下格式输出评审结果：

```
总体评价: [通过/不通过]

详细评分:
- 规范性: [分数 1-10]
- 完整性: [分数 1-10]
- 可执行性: [分数 1-10]
- 数据真实性: [分数 1-10]
- 覆盖度: [分数 1-10]

问题列表:
[如果不通过，列出所有问题，每个问题包含：用例编号、问题描述、改进建议]

改进建议:
[整体改进建议]
```
"""
        return prompt
```

**Step 4: Run tests**

```bash
uv run pytest app/tests/test_prompt_builder.py -v
```

Expected: All PASS

**Step 5: Commit**

```bash
git add backend/app/rag/prompt_builder.py backend/app/tests/test_prompt_builder.py
git commit -m "feat: 实现 RAG Prompt 构建器"
```

---

## 完成检查清单

- [ ] 向量化模型配置完成
- [ ] 测试用例支持向量存储
- [ ] 向量检索功能正常
- [ ] Few-shot Prompt 构建完成
- [ ] 所有测试通过

## 下一步

继续实施：
- `2026-03-04-04-agent-system.md` - Agent 系统（生成 Agent + 评审 Agent）
- `2026-03-04-05-document-parsers.md` - 文档解析插件
